20260916 面试记录

远程办公模式-from 电鸭

AI算法工程师
岗位职责
参与风控策略制定，基于XGBoost/LightGBM主导小模型开发与迭代，在时序场景下引入RNN/LSTM/Transformer进行行为序列建模。
解决风控场景下的样本不均衡问题，设计过采样、欠采样或代价敏感学习等组合方案；建立特征质量监控，排查特征穿越、线上离线不一致等隐患。
优化训练Pipeline，完成模型压缩与推理加速（满足毫秒级响应），负责模型从离线训练到在线部署的全链路工程落地（ONNX/PMML/API封装），并配合A/B实验灰度发布。
搭建模型监控体系，持续跟踪模型稳定性与业务核心指标（如KS、AUC），制定自动回滚与在线更新策略。
任职要求
本科及以上，计算机/数学/统计相关专业，2年以上信贷/支付/反欺诈风控模型经验。
精通XGBoost、LightGBM原理及调优，熟练使用Python/SQL处理大规模数据（Hive/Spark优先）。
扎实的机器学习理论基础，能独立排查过拟合、特征共线性、Loss不收敛等问题。
有深度学习落地经验（RNN/LSTM处理时序序列，或Transformer编码Embedding）。
具备良好工程能力，熟悉Git、Docker，有FastAPI/Flask模型服务化经验。
加分项：在顶会发表过风控/机器学习相关论文，或有AI Agent（智能体）在实际业务场景下的应用实践经验

---

**業務與項目落地**
* 涉及到大模型加載不同的 Skill，如何對 Skill 能力的監管或動態化加載嗎？

**Agent 架構與設計**

* 從 Workflow 到 Agent Loop（類似 Cursor、Claude Code 等能力），你覺得 Workflow 和 Agent Loop 的差異點在哪裏？各自優勢是什麼？
* 從 Agent Loop 的角度看，在實際應用中，有哪些場景會適合用 Loop 這種方式？
* 簡歷中提到的 Multi-Agent，每個 Agent 之間一般是怎麼做協作和通信的？
* 如果要應用 LLM 的 Function Calling 能力，一般在發送請求時把工具傳給模型，是放到哪個參數裏面？
* 大模型主流協議中除了 OpenAI 還有 Anthropic（Messages API），你覺得這兩者在傳遞 Tools 時有什麼差異嗎？
* 大模型請求中的 `temperature` 參數取值範圍和作用是怎樣的？
* 假設把 `temperature` 設置成 0，能保證同樣的輸入情況下，輸出結果一定是唯一的嗎？
* `temperature` 設置爲 0，它最終影響的是模型輸出的哪個地方？

**機器學習與風控算法**

* 在業務場景中，什麼情況下選用 LightGBM，什麼情況下選用 XGBoost？選型上有哪些考慮？
* 可以簡單說一下 LightGBM 和 XGBoost 的原理及核心差異點嗎？
* **場景題**：總共有 20 萬個樣本（19 萬正樣本、1 萬負樣本，極度不平衡），80 個特徵（98% 是離散特徵，只有 2%~3% 是連續特徵）。針對這個場景，你會怎麼進行模型選型和數據處理？
* 針對正負樣本極度不均（19:1）的情況，如果選擇 XGBoost 或 LightGBM 這一類樹模型，你會怎麼處理？
* XGBoost 內置了調節正負樣本權重的參數（如 `scale_pos_weight`），這個參數具體是影響了模型裏面的哪一個部分？爲什麼設置它是有效的？
* 樣本權重參數會影響到樹的複雜度嗎？樹的複雜度是怎麼被影響的？
* 機器學習裏的邏輯迴歸（Logistic Regression）公式和基本原理是什麼？
* 邏輯迴歸裏核心的激活函數（如 Sigmoid）是什麼？Sigmoid 函數的值域空間是多少？
* 邏輯迴歸和普通的 DNN（深度神經網絡）有什麼共性嗎？可以把邏輯迴歸理解成一種神經網絡嗎？
* 最簡單的三層神經網絡結構大概是怎樣的？
* 機器學習除了分類和迴歸，還有哪些類別的劃分？
* 常見的聚類算法有哪些？
* 能簡單說一下 KNN 算法的原理嗎？
* K-means 算法的核心思想和原理是什麼？它是如何實現聚類的？
* Softmax 函數主要是用來預測什麼的？

---

看起来比较小,结果资历大的面试官
